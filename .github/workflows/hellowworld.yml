name: "Hello world workflow"
# This will trigger when pushed on master or production branch
on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello_world"
        run: echo "hello_world"
